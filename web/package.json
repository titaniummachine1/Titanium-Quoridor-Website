{
  "name": "quoridor-ai-clone",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:wasm": "node scripts/build-wasm.mjs",
    "build:pages": "npm run build:wasm && vite build --mode ghpages",
    "preview": "vite preview",
    "preview:pages": "vite preview --mode ghpages"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
